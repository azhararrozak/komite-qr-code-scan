{
  "name": "komite-qr-code-scan",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n FRONT,BACK -c green,cyan \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install && mkdir -p backend/client && cp -r frontend/dist/* backend/client/",
    "start": "node backend/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "multer": "^2.0.2"
  }
}
